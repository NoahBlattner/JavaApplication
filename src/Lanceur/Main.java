package Lanceur;

import JAnchorFrame.*;

import java.awt.*;

public class Main {
    public static void main(String[] args) {


        TestAnchorWindow mainFrame = new TestAnchorWindow("TestAnchorWindow", new Rectangle(500, 500));


        /*
        // launch the connection window
        JFrame frame = connectionWindow.createWindow("Connection", 500, 500);

        // when the button is clicked, the window is closed
        frame.getContentPane().getComponent(0).addMouseListener(new java.awt.event.MouseAdapter() {
            @Override
            public void mouseClicked(java.awt.event.MouseEvent evt) {
                frame.dispose();
            }
        });

         */
    }

}